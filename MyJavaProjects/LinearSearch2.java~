
public class LinearSearch {

	int findElement (int[] arr, int element) {

		int pos = 0;
		boolean isFound = false;
		while (!isFound && (pos < arr.lentgh)) {
			if (field[pos] == element)
				isFound == true;
			else
				pos++;

		}
		if (isFound)
			return pos;
		else
			return -1;
	}

	public static void main(String[]args) {
		
		int[] arg = {};
		int number = 56;

		System.out.println("


		}

}
